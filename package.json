{
  "name": "phantex-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend build pipeline for Phantex -- bundles CSS and JS into phantex/static/dist/",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch",
    "dev": "vite"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  }
}
